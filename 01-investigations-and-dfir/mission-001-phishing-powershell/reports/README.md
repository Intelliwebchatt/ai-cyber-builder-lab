# Reports

Generated starter output and the analyst's final reports belong here.

`generated-analysis.md` is ignored by default so the repository owner can run the tool, inspect the output, and decide what evidence belongs in the final case file before committing results.
