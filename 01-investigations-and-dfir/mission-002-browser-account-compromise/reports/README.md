# Reports

Generated starter output belongs here.

`generated-analysis.md` is ignored by default so the repository owner can run the analyzer, inspect the output, and decide what evidence belongs in the final case file before committing results.

Generated Markdown must not be treated as a conclusion.
